import {render} from "@testing-library/react";
import ProfileButton from "../../../components/navigation/TopNav/ProfileButton";

test("renders profile button", () => {
    render(<ProfileButton/>);
});
